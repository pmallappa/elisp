;;;_.============================================================
;;;_. Undo-Tree undo visualizer

(require 'undo-tree)


;; If you want to replace the standard Emacs' undo system with the
;; `undo-tree-mode' system in all buffers
;(global-undo-tree-mode)

;; If you just want to see the undo tree occasionally
;; call M-x undo-tree-visualizer

(provide 'emacs-undo)